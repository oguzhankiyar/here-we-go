package main

import (
	"encoding/asn1"
	"math/bits"
)

func main() {
	bits.Add()
	bits.Div()
	bits.LeadingZeros()
	bits.Len()
	bits.Mul()
	bits.Rem()
	bits.TrailingZeros()
	bits.Sub()
	bits.RotateLeft()
	bits.Reverse()
	bits.ReverseBytes()
	bits.OnesCount()
}
